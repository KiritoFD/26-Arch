module device #(
	parameter logic SIMULATION = 1'b0
)(
	input logic clk, reset,
	input logic cpu_clk,

`line 9 "../../vsrc/../../vivado/src/device.sv" 0
	 
	output logic [3:0] led,
	input logic [3:0] sw,
	output logic tx,

`line 14 "../../vsrc/../../vivado/src/device.sv" 0
	 
	input logic valid,
	input logic [63:0] addr,
	input logic wvalid,
	input logic [7:0] size,
	input logic [63:0] wdata,
	output logic [63:0] rdata,

`line 22 "../../vsrc/../../vivado/src/device.sv" 0
	output logic ready,
	output logic last
);
	 
	logic [63:0] cnter, cnter1;

`line 28 "../../vsrc/../../vivado/src/device.sv" 0
	always_ff @(posedge clk) begin
		if (reset) {cnter, cnter1} <= '0;
		else begin
			cnter1 <= cnter1 + 1;
			if (cnter1 == 100) begin
				cnter1 <= '0;
				cnter <= cnter + 1;
			end
		end
	end

`line 39 "../../vsrc/../../vivado/src/device.sv" 0
	 
	logic [3:0] switch;
	always_ff @(posedge clk) begin
		switch <= sw;
	end

`line 45 "../../vsrc/../../vivado/src/device.sv" 0
	always_comb begin
		rdata = 'x;
		unique case(addr)
			SW_ADDR: begin
				unique case(switch)
					4'd0: begin
						rdata = 64'd31;
					end
					4'd1: begin
						rdata = 64'd1;
					end
					4'd2: begin
						rdata = 64'd2;
					end
					4'd3: begin
						rdata = 64'd4;
					end
					4'd4: begin
						rdata = 64'd8;
					end
					4'd5: begin
						rdata = 64'd16;
					end
					default: begin
						
					end
				endcase
			end
			COUNTER_1, COUNTER_2: begin
				rdata = cnter;
			end
			TX_READY: begin
				rdata = '0;
			end
			default: begin
				
			end
		endcase
	end

`line 85 "../../vsrc/../../vivado/src/device.sv" 0
	
	always_ff @(posedge clk) begin
		if (reset) led <= '0;
		else if (valid && wvalid && (addr == FINISH_ADDR)) led <= '1;
	end
	
	 
	assign last = ready;

`line 94 "../../vsrc/../../vivado/src/device.sv" 0
	 
	parameter BIT_TMR_MAX = 'b10100010110000;
    parameter BIT_INDEX_MAX = 10;

`line 98 "../../vsrc/../../vivado/src/device.sv" 0
	logic finish;
	always_ff @(posedge clk) begin
		if (reset) finish <= '0;
		else if (valid && addr == FINISH_ADDR && wvalid) finish <= '1;
	end

`line 104 "../../vsrc/../../vivado/src/device.sv" 0
    logic [13:0] bitTmr = '0;

`line 106 "../../vsrc/../../vivado/src/device.sv" 0
    localparam type state_t = enum logic [1:0] {
        RDY, LOAD_BIT, SEND_BIT
    };

`line 110 "../../vsrc/../../vivado/src/device.sv" 0
    logic bitDone;
    int bitIndex;
    logic txBit = '1;
    logic [9:0] txData;
    state_t txState = RDY;

`line 116 "../../vsrc/../../vivado/src/device.sv" 0
     
     
     
     
     

`line 122 "../../vsrc/../../vivado/src/device.sv" 0
    logic send;
    logic [7:0] char_data;
    logic tx_ready;

`line 126 "../../vsrc/../../vivado/src/device.sv" 0
    wire [15:0][7:0] str = {
        8'h48,8'h65,8'h6c,8'h6c,8'h6f,8'h20,
        8'h77,8'h6f,8'h72,8'h6c,8'h64,8'h21,8'ha,8'h0
    };

`line 131 "../../vsrc/../../vivado/src/device.sv" 0
    int idx = 14;
	logic putchar;
	always_ff @(posedge clk) begin
		if (reset) putchar <= '1;
		else if (~valid) putchar <= '1;
		else if (addr == TX_DATA && valid && wvalid && txState != RDY) putchar <= '0;
	end
	
	
    assign send = (idx != 0 && finish) || (addr == TX_DATA && valid && wvalid);

`line 142 "../../vsrc/../../vivado/src/device.sv" 0
    always_ff @(posedge clk) begin
        if (send && finish) begin
            if (tx_ready) idx <= idx - 1;
        end
    end

`line 148 "../../vsrc/../../vivado/src/device.sv" 0
    assign char_data = finish ? str[idx] : wdata[39:32];
    always_ff @(posedge clk) begin
        unique case(txState)
            RDY: begin
                if (send && putchar) txState <= LOAD_BIT;
            end
            LOAD_BIT: begin
                txState <= SEND_BIT;
            end
            SEND_BIT: begin
                if (bitDone) begin
                    if (bitIndex == BIT_INDEX_MAX) txState <= RDY;
                    else txState <= LOAD_BIT;
                end
            end
            default: begin
                txState <= RDY;
            end
        endcase
    end

`line 169 "../../vsrc/../../vivado/src/device.sv" 0
    always_ff @(posedge clk) begin
        if (txState == RDY) bitTmr <= '0;
        else if (bitDone) bitTmr <= '0;
        else bitTmr <= bitTmr + 1;
    end

`line 175 "../../vsrc/../../vivado/src/device.sv" 0
    assign bitDone = bitTmr == BIT_TMR_MAX;

`line 177 "../../vsrc/../../vivado/src/device.sv" 0
    always_ff @(posedge clk) begin
        if (txState == RDY) bitIndex <= '0;
        else if (txState == LOAD_BIT) bitIndex <= bitIndex + 1;
    end

`line 182 "../../vsrc/../../vivado/src/device.sv" 0
    always_ff @(posedge clk) begin
        if (send) txData <= {1'b1, char_data, 1'b0};
    end

`line 186 "../../vsrc/../../vivado/src/device.sv" 0
    always_ff @(posedge clk) begin
        if (txState == RDY) begin
            txBit <= '1;
        end else if (txState == LOAD_BIT) begin
            txBit <= txData[bitIndex];
        end
    end

`line 194 "../../vsrc/../../vivado/src/device.sv" 0
    assign tx = txBit;
	assign tx_ready = txState == RDY; 
	if (SIMULATION)
		assign ready = '1;
	else
    	assign ready = tx_ready;
		
	always_ff @(posedge clk) begin
		if (~reset && valid && wvalid) begin
			if (addr == TX_DATA) begin
				$write("%c", char_data);
			end else if (addr == FINISH_ADDR) begin
				$write("Hello World!\n");
			end
		end
	end
	
	
endmodule

